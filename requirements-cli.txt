# Minimal dependencies for the console frontend (cli.py).
# The GUI's ttkbootstrap/Pillow/pynput/scipy are not needed.
# Install: uv venv && uv pip install -r requirements-cli.txt
numpy==2.4.4
pydantic==2.13.4
requests==2.33.1
numba==0.65.1
